{
	"name": "@cyrus/proxy-worker",
	"version": "1.0.0",
	"description": "Cloudflare Workers edge proxy for Cyrus - handles Linear webhooks and OAuth",
	"main": "src/index.ts",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"preview": "wrangler versions upload",
		"tail": "wrangler tail",
		"typecheck": "tsc --noEmit",
		"setup": "./scripts/setup-wrangler.sh",
		"cleanup": "./scripts/cleanup-wrangler.sh"
	},
	"dependencies": {
		"itty-router": "^4.0.23"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240117.0",
		"typescript": "^5.3.3",
		"wrangler": "^4.22.0"
	}
}
