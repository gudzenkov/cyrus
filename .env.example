# =============================================================================
# SETUP CONFIGURATION - Choose one option below
# =============================================================================
# Handles: OAuth callbacks + Linear webhooks

# Option 1: Local development with ngrok
# Run: ngrok http 3456
# Then use the ngrok URL as CYRUS_BASE_URL
# export CYRUS_BASE_URL=https://abc123.ngrok-free.app  # OAuth callbacks + webhooks
# export CYRUS_SERVER_PORT=3456                        # Local server port

# Option 2: Self-hosted Cyrus on remote server with public domain/IP
# export CYRUS_BASE_URL=https://your-domain.com        # OAuth callbacks + webhooks
# export CYRUS_SERVER_PORT=3456                        # Server port
# export CYRUS_HOST_EXTERNAL=true                      # Listen on 0.0.0.0 for external connections

# Option 3: Managed setup using Cyrus proxy (recommended setup)
export PROXY_URL=https://cyrus-proxy.ceedar.workers.dev  # Proxy handles OAuth + webhooks

# Direct Linear OAuth configuration (optional - for self-hosted Linear OAuth)
# LINEAR_DIRECT_WEBHOOKS=true  # Enable direct webhook handling without Cyrus proxy
# LINEAR_CLIENT_ID=<your Linear OAuth app client ID>
# LINEAR_CLIENT_SECRET=<your Linear OAuth app client secret>
# LINEAR_WEBHOOK_SECRET=<your Linear webhook secret>

# Legacy environment variables (still supported for backward compatibility)
# CYRUS_WEBHOOK_BASE_URL=<url>  # Use CYRUS_BASE_URL instead
# CYRUS_WEBHOOK_PORT=3456  # Use CYRUS_SERVER_PORT instead

# Claude Code models
# export CYRUS_DEFAULT_MODEL=sonnet
# export CYRUS_DEFAULT_FALLBACK_MODEL=haiku
